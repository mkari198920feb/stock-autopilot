Drop dashboard PNGs here when you want them in the main README.

Suggested names: `dashboard-overview.png`, `market-pulse.png`, `email-preview.png`.

```bash
python main.py autopilot --port 8080
# screenshot http://127.0.0.1:8080 → save here
```

Then in root README: `![dashboard](docs/screenshots/dashboard-overview.png)`
